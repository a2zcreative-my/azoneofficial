{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "//": "v1.100.1 - 'Claude outputs' is where the desktop app drops files it could not place, and it is INSIDE this project. next build type-checks everything tsconfig includes, so one stray worker file in there (hotels.ts, whose imports resolve from worker/src) failed the whole website build after the API had already published. Excluded here so a copy of a file can never break the build again. The folder is safe to delete: everything in it is a duplicate of a file that lives somewhere else in this project.",
  "exclude": ["node_modules", "worker", "Claude outputs", "Claude outputs/**"]
}
